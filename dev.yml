version: "3.9"

services:
  postgres:
    image: postgres:14-alpine
    env_file:
      - ".env"
    ports:
      - "5432:5432"

  redis:
    image: redis
    environment:
      - decode_responses=True
    ports:
      - "6379:6379"

